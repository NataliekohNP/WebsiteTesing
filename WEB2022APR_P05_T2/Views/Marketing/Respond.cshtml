@model WEB2022APR_P05_T2.Models.Response

@{
    ViewData["Title"] = "Respond";
}

<h1>Respond</h1>

<hr />
<h4>Comment Information</h4>
    <div class="col-sm-9 form-control-plaintext">
    @Html.Display("MemberID")</div>
    <div class="col-sm-9 form-control-plaintext">
        @Html.Display("UserFeedback")</div>
    <div class="row">
        <div class="col-md-4">
            <form asp-action="Respond">
                <div class="form-group">
                    <label name="Text" class="control-label">Enter Your Response: </label>
                    <input id="Text" name="Text" class="form-control" />
                </div>
                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>

    <div>
        <a asp-action="Index">Back to List</a>
    </div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    }
